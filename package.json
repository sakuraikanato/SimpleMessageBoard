{
    "name": "message-board",
    "private": true,
    "scripts": {
        "dev:all": "bun run --parallel --filter='*' dev",
        "dev:web": "bun run --filter web dev",
        "dev:api": "bun run --filter api dev"
    },
    "workspaces": [
        "apps/*"
    ]
}