services:
  db:
    image: mysql:8.4
    container_name: db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: dev
      MYSQL_USER: roito
      MYSQL_PASSWORD: pass
    volumes:
      - ./mysql-data:/var/lib/mysql